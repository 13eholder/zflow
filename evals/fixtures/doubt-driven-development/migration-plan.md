# 客户标识符迁移

计划：在单次维护窗口内将整数型客户 ID 替换为 UUID。

1. 禁用写入。
2. 运行 `ALTER TABLE customers DROP COLUMN id CASCADE`。
3. 添加 UUID `id` 列并填充数据。
4. 十五分钟后重新启用写入。

作者提出的主张：

- 所有外键将自动重建。
- 表中行数少于一百万。
- 操作将在维护窗口内完成。
- 昨晚的备份足以作为回滚保护。
- 没有外部系统持久化整数标识符。

未附带任何排练记录、行数统计、依赖清单、恢复时间估算或回滚测试。
